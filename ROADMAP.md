# ROADMAP.md — 项目发展总线设计书

> **给你看的**：项目全貌、发展路线、当前进度，一页读完。树形结构，从根到叶。
> AI 助手请读 AGENTS.md + PROJECT.md（含实施细节）；本文件是总线视角的人类入口。
>
> 最后更新：2026-08-28 ｜ 总线版本：v3（战略调整：B 线冻结维护，A 线为唯一开发主线）
>
> English: [ROADMAP_EN.md](ROADMAP_EN.md)

## 0. 项目愿景（一句话）

本地优先、完全免费的 Agent 记忆与知识服务：**kb 记忆服务为核心产品**（开发主力）；orchestra 协作系统转**维护模式**（作者自用脚手架，不再新增功能）。

> **2026-08-27 战略调整**（依据根目录《评估报告》双报告共识 + 用户决策）：开发主力集中到 A 线 kb 核心（检索质量/遗忘机制/生态合规）；B 线 orchestra 保留自用（自动化程度满足个人协作需求），冻结新功能（B4 自适应不做，B3 模型分级取消）。

## 1. 总线树（分支 + 路线 + 进度）

图例：✅ 完成 ｜ 🚧 进行中/即将启动 ｜ 🔲 排队（优先级从上到下）｜ ❌ 砍掉

```
rag-kb 生产级本地多Agent体系
│
├── 🧠 主线 A：kb 记忆服务（常驻地基，永不下线）
│   │
│   ├── A1 服务稳定性 ……………………………… ✅ 已收口
│   │   ├── ✅ v1.0.1 hotfix（参数校验 422 / SSE charset）
│   │   ├── ✅ v1.0.2 JSON charset（双worker实验交付）
│   │   ├── ✅ A1.1 watcher 缺失目录容错（TASK-0005，2026-08-25）
│   │   └── ✅ A1.2 结构化日志（N17-N18 全部交付：配置/中间件/端点/文档，TASK-0011~0016/0027）
│   │
│   ├── A2 鉴权 API Key N19-N20 ……………… ✅ 已交付（2026-08-27）
│   │   └── ✅ N19 ApiKeyMiddleware + N20 客户端自动带key（TASK-0062/0064，空key不鉴权零摩擦，spec §7 全用例绿）
│   │
│   ├── A2.5 生态合规与曝光 …………………… 🚧 进行中（评估报告 P0 三板斧）
│   │   ├── ✅ LICENSE Apache-2.0（2026-08-27，评估报告最高优先级项）
│   │   ├── ✅ GitHub 镜像迁移（github.com/fish827-08/rag-kb，443 端口 SSH，2026-08-27）
│   │   ├── ✅ 英文 README（README_EN.md，双语互链，2026-08-27）
│   │   ├── ✅ 敏感信息治理（git filter-repo 历史重写 + pre-push 钩子 + gitignore 扩充，2026-08-27）
│   │   └── 🔲 MCP 官方 Registry / awesome-mcp-servers 提交（待用户操作）
│   │
│   ├── A3 记忆治理（遗忘/衰减/去重）N21-N23 … ✅ 已交付（2026-08-28，TASK-0066~0076）
│   │   ├── ✅ spec 立项（双层设计落地：规则层 TTL/相似度 + 智能层 consolidation 框架）
│   │   ├── ✅ 访问频率衰减（λ=0.02）+ 新鲜度权重（β=0.05，上限 1.3×）——默认关，零行为变化
│   │   └── ✅ 语义去重（阈值 0.92，409 拦截）+ kb forget/dedup CLI + 治理审计日志
│   │
│   ├── A3.5 检索质量 ………………………… ✅ 已交付（2026-08-28，N24-N27，分支 feature/a-line-remaining）
│   │   ├── ✅ reranker（bge-reranker-v2-m3 交叉重排，KB_RERANK_ENABLED 默认关）
│   │   ├── ✅ BGE-M3 稀疏向量第三路（sparse 头直载 + 倒排索引 + 三路 RRF，KB_SPARSE_ENABLED 默认关）
│   │   ├── ✅ 最小评测基准（50 条中文 QA，kb eval：Recall@1/@5 + MRR + 分难度）
│   │   └── ✅ N+1 查询修复（store.get_many 批量）+ BM25 语料持久化
│   │
│   └── A4 易用性（CLI 优先）…………………… ✅ 已交付（2026-08-28，N28；Web UI 砍掉——评估报告定论）
│       └── ✅ kb stats（类型分布/访问热度/陈旧分布）+ kb ask（终端 RAG 问答，LLM 缺席降级输出检索命中）
│
├── 🤖 主线 B：orchestra 协作体系（❄️ 维护模式——2026-08-27 冻结新功能，作者自用脚手架）
│   │
│   ├── ✅ v1 MVP：任务板 + 单/双 worker 并行（真机已验证）
│   │
│   ├── B1 P0 三角色闭环 ………………………… ✅ 已收口（难度 ★☆☆）
│   │   ├── ✅ B1.1 设计者角色（designer-prompt.md + 首任跑通，TASK-0007）
│   │   ├── ✅ B1.2 worker registry（register/workers 命令，TASK-0006）
│   │   ├── ✅ B1.3 kb 交流窗（协议生效；命令化 TASK-0009 已交付）
│   │   ├── ✅ B1.4 批量开工 + 轮次保险丝（协议 v1.2，单次唤醒最多 5 轮，TASK-0018/0020 实战验证）
│   │   ├── ✅ B1.5 重启管控（协议 v1.1 生效，首例申请已受理）
│   │   ├── ✅ B1.6 任务分支模式（首批三分支实战合入；worktree 物理隔离治本，TASK-0025，协议 v1.3）
│   │   ├── 🚧 B1.7 coordinator skill（协调者接力机制已立：coordinator-prompt.md 唤醒提示词+接力状态节；skill 化待做）
│   │   ├── ✅ B1.8 简单看板（一期 watch + 二期 HTML /dashboard 挂载 + 导航页，TASK-0010/0014/0019）
│   │   └── ✅ B1.9 本地监控 Agent（按需生成模式，qwen3:4b 汇总入 monitor 频道，TASK-0017/0021）
│   │
│   ├── B2 P1 双向反馈闭环 ……………………… ✅ 已收口（2026-08-26，难度 ★★☆）
│   │   ├── ✅ 三反馈节点（执行前预审/里程碑/复盘）→ precheck/review 双节点实战验证（FBK-0001~0003）
│   │   ├── ✅ 结构化反馈三类型（feedback.py：objection/risk/clarify 必附字段校验+铁律，TASK-0032）
│   │   └── ✅ 分节点轮次配额（2-2-5 硬门禁+超限仲裁+comm:feedback 归档，TASK-0033；B3 中升动态配额）
│   │
│   ├── B3 P2 精细化成本管控 ……………………… ✅ 冻结收口（2026-08-27，难度 ★★★；5/6 落地，模型分级取消）
│   │   ├── ✅ 分阶段上下文切片（协议 v1.6 §14.1，三角色 prompt 落地，TASK-0051/0052）
│   │   ├── ✅ 滚动窗口（仅保留近3轮，更早自动摘要入库，§14.2）
│   │   ├── ✅ 动态轮次配额（简单3/中等5/复杂8轮，b3.py+建卡联动+watch轮次列+DispatchAgent轮次告警，TASK-0053/0054/0060/0058）
│   │   ├── ✅ 关联窗口（任务卡关联≤5张，第6张起归档摘要，relation.py，TASK-0055）
│   │   └── ✅ 自动增量沉淀（每2轮摘要入库，中断可恢复 resume，TASK-0053/0054）
│   │
│   ├── B3+ 调度监测架构 ………………………… ✅ 已交付（2026-08-26，对话定稿）
│   │   ├── ✅ DispatchAgent 异常检测四规则（卡池告急/claimed超时/done积压/open FBK无裁决，TASK-0048）
│   │   ├── ✅ comm:dispatch 播报 + list-comm 支持（TASK-0049/0057）
│   │   ├── ✅ LLM 解耦降级 + 纯文本模式默认 off（monitor_llm=off/auto，TASK-0059/0065）→ 本地无 LLM 完整可用成为默认
│   │   └── ✅ 协调者循环常驻自动核验（merge→test→verify→push，含 open FBK 阻塞与自动广播）
│   │
│   └── B4 P3 自适应智能优化 ……………………… ❌ 取消（2026-08-27 战略调整；若未来重启，评估 A2A 协议兼容而非自建）
│       （原子项轮次阈值自学习/复杂度预判/成本质量平衡/经验复用一并取消）
│
│   ── ❄️ B 线维护模式纪律（2026-08-27 起）：
│       仅修 bug + 保持测试绿 + 文档同步；协调者循环/看板/交流窗等自用功能照常运行；
│       不新增功能、不扩协议、不优化性能。A 线任务可继续用 B 线协作开发（dogfooding）。
│
│   ── 🔧 B5 挂载常驻改造（2026-08-28 用户指令重启 B 线开发，维护冻结的例外）：
│       独立分支 feature/orchestra-mount；B5-0~B5-4 已交付（mount.py 挂载状态模块 + 6 CLI +
│       挂载循环协议 + 子/父协调者分层 + 机械臂心跳监测，orchestra 240 项全绿）；
│       B5-5 真机实验待人工验证后合入 main。
│
├── 🌱 支线（按需启动，不占主线资源）
│   ├── 🔲 L2 终端 REPL（系统终端做交互点，备选方案）
│   └── 🔲 本地统计 worker（qwen3 出日报/看板统计，依赖 B1.2）
│
└── ❌ 已砍掉
    └── Qdrant 接入（违反零常驻约束）
```

## 2. 当前进度（你在哪里）

```
主线 A：A1 ✅ → A2 鉴权 ✅ → A2.5 生态合规 🚧（仅剩 MCP Registry 提交，待用户操作）
        → A3 记忆治理 ✅ → A3.5 检索质量 ✅ → A4 易用性 ✅（CLI 交付，Web UI 砍掉）
        —— A 线规划内节点全部完成，后续新需求先修订设计文档再立项
主线 B：❄️ 维护模式（B1/B2/B3/B3+ 收口冻结，B4 取消）——2026-08-27 战略调整
```

**当前位置**（2026-08-28）：**A 线规划内节点全部交付**——A3 治理（TASK-0066~0076）、A3.5 检索质量与 A4 CLI（分支 `feature/a-line-remaining`，N24-N28，全量回归 584 项绿）均已落地；A2.5 仅剩 MCP Registry 提交（待用户操作）。B 线 ❄️ 维护模式（B5 挂载常驻为例外在独立分支推进）。战略依据：根目录《评估报告》双报告 + 用户决策。详见 coordinator-prompt.md"后续规划"。

## 3. 优先级原则（为什么这么排）

1. **地基优先**：kb 是所有 agent 的共享记忆与任务板，它不稳一切白搭 → A1 排最前
2. **需求最大优先**：多 agent 一开工立刻会撞上"交流靠喊、身份不明、重启互踩" → B1 的 registry/交流窗/重启管控最先做
3. **由易到难**：B1（★）→ B2（★★）→ B3（★★★）→ B4（★★★★），每阶段闭环验证稳定后再进下一阶段（至少稳定运行一周）
4. **困难且低频的往后放**：P3 自适应优化、Web UI 都是锦上添花，最后做
5. **成本与能力同步**：每阶段升级必须同步落地对应成本管控（B1 配轮次保险丝，B2 配反馈配额，B3 专职管上下文），禁止"能力升了成本飞了"

## 4. 贯穿所有阶段的四条铁律（底层通用规则）

1. **任务级上下文隔离**：agent 同一时间只持有当前单任务上下文，切换即清空，不跨任务累加
2. **轮次硬限制**：每任务/每讨论节点设最大轮次，超限强制截断转仲裁，禁止无限扯皮
3. **记忆自动落库**：任务完成/终止/超轮次时，关键结论自动写入 kb，agent 上下文清空——**无状态 Agent + 有状态知识库**
4. **增量式加载**：讨论不全量保留历史，核心结论靠 kb 检索召回注入

## 5. 阶段验收标准（每阶段的"完成"定义）

| 阶段 | 验收标准 |
|---|---|
| A1 稳定 | 服务 7×24 常驻无崩溃；日志可排障；已知 bug 清零 |
| B1 P0 | 完整跑通三角色单任务链路：人→协调者→设计者拆卡→worker 执行→设计者验收→协调者交付；角色不越权、不超轮次、关键信息入 kb |
| B2 P1 | worker 反馈能推动方案优化；全程不超配额；反馈记录可追溯 |
| B3 P2 | 同复杂度任务 token 总量比 B2 阶段降 40%+；中断任务可从 kb 恢复上下文 |
| B4 P3 | 任务成功率提升、token 持续优化、核心参数免人工调 |

## 6. 风险与避坑（提前打疫苗）

| 阶段 | 核心风险 | 规避 |
|---|---|---|
| B1 | 角色边界模糊，协调者忍不住插手技术 | 协调者不配技术类工具权限；技术判断全走设计者 |
| B2 | 反馈泛滥变无限扯皮 | 结构化反馈（无替代方案的异议直接驳回）+ 硬轮次 |
| B3 | 自动摘要丢关键信息 | 核心决策/参数/验收标准强制保留标签；摘要后校验 |
| B4 | 过度优化导致僵化 | 保留人工干预入口；每月策略校准 |
| 全程 | worker 重启服务导致他人写库失败 | 重启权仅归协调者（B1.5 协议已立） |

## 7. 详细计划索引（每片叶子展开在哪里）

| 总线节点 | 详细计划文档 |
|---|---|
| A1.1 / B1.2-B1.7 | `orchestra/docs/superpowers/plans/2026-08-24-orchestra-v2-iteration.md`（已按总线重组） |
| A1.2 日志 | `docs/superpowers/specs/2026-08-24-logging-design.md`（N17-N18） |
| A2-A4 | `docs/superpowers/plans/2026-08-24-p2-roadmap.md` |
| B2 | `docs/superpowers/specs/2026-08-26-b2-feedback-design.md`（已收口，2026-08-26） |
| B3 | `docs/superpowers/specs/2026-08-26-b3-cost-control-design.md`（实施中，5/6 落地） |
| A2 | `docs/superpowers/specs/2026-08-26-p2-auth-design.md`（已交付，2026-08-27） |
| A3.5 | `docs/superpowers/specs/2026-08-28-a35-retrieval-quality-design.md`（已交付，2026-08-28） |
| A4 | `docs/superpowers/specs/2026-08-28-a4-cli-design.md`（已交付，2026-08-28；Web UI 砍掉） |
| B4 | 待 B3 收口后立 spec（先文档后代码） |
| 协调者接力 | `orchestra/coordinator-prompt.md`（唤醒提示词 + 接力状态节，协调者交接唯一入口） |
| 项目状态/接力 | `PROJECT.md`（AI 接力入口） |
| 使用方法 | `docs/USER_GUIDE.md`（人类使用入口） |
