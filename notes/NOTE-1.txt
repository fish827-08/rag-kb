task1
关于pathlib的用法
主要有
Path.cwd()                  # 当前工作目录
Path.suffix      # '.gz'             最后一个后缀
exists() 判断文件是否存在
Path.cwd().parent， parent得到父路径
等等
以及.env加载环境变量的用法

task2
python导包的用法，基础if和for的用法回顾
Document的构成
抛出异常的用法
list按想要的间隔切片用法
fmt = "/".join(self.supported_formats)
切片supported_formats以"/"间隔
如何切片，切片document
if __name__ == "__main__":的使用
类方法的调用和类静态方法创建（好像就是不传self参数，现在没用到）
类的局部变量以及全局变量的位置以及使用，在类构造方法"__init__"中初始化局部变量